# -*- mode: snippet -*-
# name: setup for Org files
# key: !setup
# --

# setup
src_emacs-lisp[:results raw]{(concat "#+setupfile: " (expand-file-name "org/${1:latex}/${2:latex-apa}.org" user-emacs-directory))}

\clearpage

$0
